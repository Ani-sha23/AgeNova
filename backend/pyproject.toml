[project]
name = "agenova"
version = "0.1.0"
description = "Autonomous multi-agent intelligence ecosystem with hybrid memory"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
